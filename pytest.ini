[pytest]
pythonpath = src
testpaths = tests
asyncio_mode = auto

markers =
    unit: быстрые изолированные тесты без внешних сервисов
    integration: требуют запущенные Postgres/Valkey/LuaWorker

addopts = -ra -q
